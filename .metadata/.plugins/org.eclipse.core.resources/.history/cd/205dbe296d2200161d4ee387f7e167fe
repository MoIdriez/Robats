/*
 * gamepad.cpp
 *
 *  Created on: 25 May 2016
 *      Author: midries
 */

#include "gamepad.h"

gamepad::gamepad() { openGamepad("/dev/input/js0"); }
gamepad::gamepad(std::string gamepadPath) { openGamepad(gamepadPath); }
gamepad::gamepad(int gamepadNumber) {
	std::stringstream sstm;
	sstm << "/dev/input/js" << gamepadNumber;
	openGamepad(sstm.str());
}

gamepad::~gamepad() {
	// TODO Auto-generated destructor stub
}

//gamepad();
//	gamepad(int gamepadNumber);
//	gamepad(std::string gamepadPath);
//	virtual ~gamepad();
//private:
//	bool isFound();
//	void openGamepad();
//	bool sample();
