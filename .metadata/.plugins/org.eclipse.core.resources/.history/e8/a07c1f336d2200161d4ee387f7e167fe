/*
 * gamepad.h
 *
 *  Created on: 25 May 2016
 *      Author: midries
 */

#ifndef GAMEPAD_H_
#define GAMEPAD_H_

class gamepad {
public:
	gamepad();
	gamepad(int gamepadNumber);
	gamepad(std::string gamepadPath);
	virtual ~gamepad();
private:
	bool isFound();
	void openGamepad();
	bool sample();
};

#endif /* GAMEPAD_H_ */
